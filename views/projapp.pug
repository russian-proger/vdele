doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    base(href="/")
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" />
    title ВДеле Проект
  body
    #root
    script.
      window.user_info = JSON.parse(new DOMParser().parseFromString("#{user}", 'text/html').body.textContent);
      window.project_info = JSON.parse(new DOMParser().parseFromString("#{project}", 'text/html').body.textContent);
    script(src="/dist/project_app.js")